[project]
name = "rl-hardware-calibration-lab"
version = "0.1.0"
description = "RL vs. classical search strategies for simulated hardware calibration."
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = ["numpy>=1.26"]

[tool.pytest.ini_options]
testpaths = ["tests"]
